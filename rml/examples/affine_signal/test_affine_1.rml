signal s;;
signal x;;

let rec process f = !emit x;;
(* let process should_not_type = run f || run f;; *)

let process envoyer s = !emit s ;;

let process main () =
  run (envoyer s) (*|| run (envoyer s)*);;

let process test = run (main ()) || run (main());;
